/** @type {import('next').NextConfig} */
const nextConfig = {
  // webpack: (config) => {
  //   config.module.rules?.push({
  //     test: /src\/app\/api/,
  //     loader: "ignore-loader",
  //   });
  //   return config;
  // },
};

module.exports = nextConfig;
